# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/formcreator"

# Secret used to sign JWTs — use a long random string in production
JWT_SECRET="change-me"

# Port the NestJS server listens on (optional, defaults to 3000)
PORT=3000
